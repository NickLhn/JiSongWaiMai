<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jswm.mapper.BizOrderItemMapper">
    <resultMap id="BaseResultMap" type="com.jswm.entity.BizOrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="dish_id" property="dishId"/>
        <result column="dish_name" property="dishName"/>
        <result column="dish_image" property="dishImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, dish_id, dish_name, dish_image, price, quantity, subtotal, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_order_item
        WHERE id = #{id}
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_order_item
        WHERE order_id = #{orderId}
        ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="com.jswm.entity.BizOrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_order_item (order_id, dish_id, dish_name, dish_image, price, quantity, subtotal)
        VALUES (#{orderId}, #{dishId}, #{dishName}, #{dishImage}, #{price}, #{quantity}, #{subtotal})
    </insert>

    <insert id="insertBatch">
        INSERT INTO biz_order_item (order_id, dish_id, dish_name, dish_image, price, quantity, subtotal)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.dishId}, #{item.dishName}, #{item.dishImage}, #{item.price}, #{item.quantity}, #{item.subtotal})
        </foreach>
    </insert>

    <delete id="deleteByOrderId">
        DELETE FROM biz_order_item WHERE order_id = #{orderId}
    </delete>
</mapper>
